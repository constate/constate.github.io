import Router from 'router/Router';
import { GlobalStyle } from 'styles/GlobalStyles';

const App = () => {
    return (
        <>
            <GlobalStyle />
            <Router />
        </>
    );
};

export default App;
