import { Reset } from 'styled-reset';
import { HomePage } from './pages/HomePage';

const App = () => {
    return (
        <div>
            <Reset />
            <HomePage />
        </div>
    );
};

export default App;
